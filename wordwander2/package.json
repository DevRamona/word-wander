{
  "name": "wordwander",
  "version": "1.0.0",
  "description": "Language learning platform",
  "main": "main.js",
  "scripts": {
    "start": "concurrently \"npm run sass\" \"npm run serve\" \"npm run api\"",
    "sass": "sass --watch scss:css",
    "serve": "live-server --port=5500",
    "api": "json-server --watch db.json --port 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "jquery": "^3.7.1",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "sass": "^1.71.1"
  }
}
